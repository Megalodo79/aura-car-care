import {sql} from 'drizzle-orm';
import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const orders=sqliteTable('orders',{id:text('id').primaryKey(),userId:text('userId').notNull(),customer:text('customer').notNull(),phone:text('phone').notNull(),car:text('car').notNull(),plate:text('plate').notNull(),service:text('service').notNull(),slot:text('slot').notNull(),bay:integer('bay').notNull(),status:text('status').notNull().default('booked'),total:integer('total').notNull(),proposedTotal:integer('proposedTotal'),proposalNote:text('proposalNote'),paid:integer('paid').notNull().default(0),employee:text('employee'),intake:text('intake'),mileage:text('mileage'),createdAt:text('createdAt').notNull(),updatedAt:text('updatedAt').notNull(),version:integer('version').notNull().default(1)},t=>[index('orders_user').on(t.userId),index('orders_slot').on(t.slot),uniqueIndex('orders_active_bay').on(t.bay).where(sql`${t.status} IN ('accepted','washing','quality')`)]);
export const reservations=sqliteTable('reservations',{id:text('id').primaryKey(),orderId:text('orderId').notNull().references(()=>orders.id),slot:text('slot').notNull(),bay:integer('bay').notNull()},t=>[uniqueIndex('reservations_slot_bay').on(t.slot,t.bay),index('reservations_order').on(t.orderId)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),orderId:text('orderId').notNull().references(()=>orders.id),actor:text('actor').notNull(),status:text('status').notNull(),note:text('note').notNull(),createdAt:text('createdAt').notNull()},t=>[index('events_order').on(t.orderId)]);
export const reviews=sqliteTable('reviews',{id:text('id').primaryKey(),orderId:text('orderId').notNull().references(()=>orders.id),userId:text('userId').notNull(),name:text('name').notNull(),rating:integer('rating').notNull(),body:text('body').notNull(),approved:integer('approved').notNull().default(0),createdAt:text('createdAt').notNull()},t=>[uniqueIndex('reviews_order').on(t.orderId),index('reviews_approved').on(t.approved)]);
export const staff=sqliteTable('staff',{email:text('email').primaryKey(),name:text('name').notNull(),role:text('role').notNull()});
export const settings=sqliteTable('settings',{id:integer('id').primaryKey(),value:text('value').notNull()});
